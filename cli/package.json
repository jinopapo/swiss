{
  "name": "swiss",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "swiss": "dist/index.js"
  },
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "lint": "echo 'no lint yet'"
  },
  "dependencies": {
    "@swiss/core": "file:../core",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}